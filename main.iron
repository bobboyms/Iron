        fn main() {
           let inline:fn = (a:int):int -> a * 8

           let block:fn = (func:fn (x:fn):int):int -> {
                func(x:inline)
                return 0
           }

           let func:fn = (x:fn):int -> 2 * 2
           block(func:func)
        }