import std.output.printf

        fn main():int {

            let x:float = 3.1
            let y:int = 30

            if (x > y) {
                printf(format:"X > Y\n")
                if (true) {
                    printf(format:"É true\n")
                }
            } else if (false) {
                printf(format:"X < Y\n")
            } else {
                printf(format:"Entrou no else\n")
            }

            printf(format:"Terminou\n")

            return 0
        }